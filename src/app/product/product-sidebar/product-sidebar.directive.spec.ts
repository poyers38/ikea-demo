import { ProductSidebarDirective } from './product-sidebar.directive';

describe('ProductSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
